# Messages
prefix: "<gray>[<#4287f5>HideCommands<gray>]"
no-permission: "{PREFIX} <red>You do not have permission!"
reload: "{PREFIX} <green>Plugin reloaded!"

# Commands section
default:
  - cmd1
  - cmd2
  - cmd3

# hidecommands.group.<group>
groups:
  vip:
    commands:
      - cmd4
  vip1:
    commands:
      - cmd5
  vip2:
    commands:
      - cmd6